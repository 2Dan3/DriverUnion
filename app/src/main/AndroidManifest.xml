<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/car_default_filler"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DriverUnison"
        tools:targetApi="31">
        <activity
            android:name=".CustomSplashScreenActivity"
            android:exported="true">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".CarsActivity"
            android:exported="false"
            android:label="@string/title_activity_car_manufacturers"
            android:theme="@style/Theme.DriverUnison" />
        <activity
            android:name=".CarDetailedActivity"
            android:exported="false"
            android:label="@string/title_activity_car_detailed"
            android:theme="@style/Theme.DriverUnison" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DriverUnison.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>